# 快速开始指南

## 5分钟快速体验

### 1. 克隆项目

```bash
git clone https://github.com/JY226622/WGAN-GP.git
cd WGAN-GP
```

### 2. 安装依赖

```bash
# 创建虚拟环境（推荐）
python -m venv venv
source venv/bin/activate  # Linux/Mac
# 或 venv\Scripts\activate  # Windows

# 安装依赖
pip install -r requirements.txt
```

### 3. 准备数据

将二次元图片放入 `data/raw/` 目录：

```
data/raw/
  ├── image1.jpg
  ├── image2.png
  └── ...
```

### 4. 预处理数据

```bash
python src/preprocess.py --input data/raw/ --output data/processed/
```

### 5. 训练模型

```bash
# 开始训练
python src/train.py

# 监控训练（另开终端）
tensorboard --logdir=runs/
```

### 6. 生成图片

```bash
# 生成1000张图片
python src/generate.py --model checkpoints/saved_models/best_model.pth \
                       --num_images 1000 \
                       --output data/generated/

# 生成插值序列
python src/generate.py --model checkpoints/saved_models/best_model.pth \
                       --mode interpolate \
                       --interpolate_steps 20
```

### 7. 评估质量

```bash
python src/evaluate.py --model checkpoints/saved_models/best_model.pth \
                       --real_dir data/processed/ \
                       --num_samples 1000
```

## 常见问题

### Q: 没有GPU怎么办？
A: 可以在 `config/config.yaml` 中设置 `use_cuda: false`，但训练会很慢。

### Q: 训练要多久？
A: 在RTX 3060上，约20-30小时（200 epochs）。

### Q: 生成的图片模糊？
A: 需要训练更多epoch或调整网络架构。

### Q: 数据集从哪里获取？
A: 
- [Anime Face Dataset](https://www.kaggle.com/datasets/splcher/animefacedataset)
- [Danbooru2019](https://www.gwern.net/Danbooru2019)
- 自行收集二次元图片

## 更多信息

详细文档请参考：
- [完整README](README.md)
- [技术文档](docs/技术文档.md)
- [项目报告](docs/项目报告.md)
- [Jupyter演示](notebooks/demo.ipynb)

## 交流与反馈

遇到问题？
1. 查看文档
2. 提交 Issue
3. 查看常见问题

祝使用愉快！🎉
